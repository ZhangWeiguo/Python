sudo apt-get install python-pip
python -m pip install --upgrade pip
python -m pip install web.py
python -m pip install numpy
python -m pip install pandas
python -m pip install sklearn
python -m pip install matplotlib
python -m pip install flask
python -m pip install mysql-connector
python -m pip install poster
sudo apt-get install nginx
sudo apt-get install uwsgi-plugin-python
# 有了上面那个，下面这个就不用了，且需要加上plugins=python的参数
# sudo apt-get install uwsgi
sudo apt install git
git init
git config --global user.name "Zhangweiguo"
git config --global user.email "zhangweiguo717@163.com"
git remote add wego git@github.com:ZhangWeiguo/Python.git
git clone https://github.com/ZhangWeiguo/Python.git
ssh-keygen -t rsa -b 4096 -C "zhangweiguo717@163.com"
cat /home/ubuntu/.ssh/id_rsa.pub # 写入github


uwsgi 用一般用户权限运行
nginx 用root用户运行
